# Policy samples

These are real public healthcare policy excerpts used as POC input. Each `.txt` file holds the raw policy text and has a matching `.meta.json` sidecar for source attribution. Sources are cited in the written report's bibliography.

Paste real policy text into the `.txt` placeholders and fill in the metadata fields before running the conversion pipeline.
